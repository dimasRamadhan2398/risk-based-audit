$kind: http-request
name: List Activity Plans
method: GET
url: '{{auth_base_url}}/api/v1/audit-activity-plans'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
