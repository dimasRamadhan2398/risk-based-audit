$kind: http-request
name: List Interviews
method: GET
url: '{{auth_base_url}}/api/v1/audit-fieldwork/interviews'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
queryParams:
  - key: assignment_letter_id
    value: ''
