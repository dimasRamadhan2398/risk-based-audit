$kind: http-request
name: List Observations
method: GET
url: '{{auth_base_url}}/api/v1/audit-fieldwork/observations'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
queryParams:
  - key: assignment_letter_id
    value: ''
