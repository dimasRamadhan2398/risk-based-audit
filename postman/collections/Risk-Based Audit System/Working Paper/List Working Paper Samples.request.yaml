$kind: http-request
name: List Working Paper Samples
method: GET
url: '{{auth_base_url}}/api/v1/working-papers/samples'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
