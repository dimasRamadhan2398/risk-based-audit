$kind: http-request
name: List Working Paper Headers
method: GET
url: '{{auth_base_url}}/api/v1/working-papers/headers'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
queryParams:
  - key: assignment_letter_id
    value: ''
