$kind: http-request
name: 'List Working Paper Causes (AOI & RCA)'
method: GET
url: '{{auth_base_url}}/api/v1/working-papers/causes'
order: 7000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
