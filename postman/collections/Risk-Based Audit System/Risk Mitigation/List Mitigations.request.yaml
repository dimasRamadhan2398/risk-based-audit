$kind: http-request
name: List Mitigations
method: GET
url: '{{auth_base_url}}/api/v1/risk-mitigations'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
queryParams:
  - key: risk_id
    value: ''
