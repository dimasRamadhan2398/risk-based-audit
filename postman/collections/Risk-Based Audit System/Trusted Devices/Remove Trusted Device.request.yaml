$kind: http-request
name: Remove Trusted Device
method: DELETE
url: '{{auth_base_url}}/api/v1/devices/:device_id'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
pathVariables:
  - key: device_id
    value: device-uuid-here
